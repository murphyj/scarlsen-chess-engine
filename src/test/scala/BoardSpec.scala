import collection.mutable.Stack
import org.scalatest._

class BoardSpec extends FlatSpec with Matchers {

    "A Board" should "be initialised with an empty state of 64-bit integers" in {
        
    }
}
